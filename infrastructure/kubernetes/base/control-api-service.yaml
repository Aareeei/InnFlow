apiVersion: v1
kind: Service
metadata:
  name: control-api
  labels:
    app.kubernetes.io/name: control-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: control-api
  ports:
    - name: http
      port: 4000
      targetPort: http
